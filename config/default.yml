App:
  domain: localhost
  port: 3030

assets:
  minify: false
  cdn:
    protocol: http
    cnames: ['localhost']
    pathPrefix: ''

api:
  default: 
    host: localhost:3030
    protocol: http
  ts-api:
    host: localhost:3031
    protocol: http