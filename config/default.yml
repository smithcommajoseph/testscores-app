app:
  domain: localhost
  port: 3030

rendrApp:
  howdy: "ohai!"

assets:
  minify: false
  cdn:
    protocol: http
    cnames: ['localhost']
    pathPrefix: ''

api:
  default: 
    host: localhost:3030
    protocol: http
  ts-api:
    host: sheltered-hamlet-5481.herokuapp.com
    # host: localhost:5000
    protocol: http
